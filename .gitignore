node_modules
out/
dist/
# Local copies of a previous renderer build kept while chasing a fix. Several
# MB each and reproducible with `npm run build:all`, so never committable.
out.bak*/
.upload-test/
release/
*.log
.DS_Store
*.tsbuildinfo
.refs/
.env
.env.*
electron-builder.env
~$*

# Local ad-hoc e2e driver scripts (not pushed); the folder and README are tracked, the scripts are not
/scripts/drivers/driver.*.mjs
/probe*.mjs
/apps/shell/build/THIRD-PARTY-NOTICES.txt
/example/
/.task/
/docs-archive/
.idea/
.claude/

# Local build artifacts dropped in the repo root
/*.dmg
/*.exe
/*.zip
/windows/

# Playwright artifacts
/test-results/

.cursor/rules/git-remote.mdc
__pycache__/
/notes/
/MEMORY.md

# Local fidelity-run scratch space: production repro documents and pack caches.
# Never committable — a stray `git add -A` would push user files public.
/working_dir/

# One-off verification tests kept while chasing a fix; promote to a real test
# file (or delete) instead of committing them under this name.
_scratch-*
# >>> Comet managed project state >>>
!/.comet/
/.comet/*
!/.comet/config.yaml
# <<< Comet managed project state <<<

# Local agent/editor tooling (machine-specific absolute paths)
/.agents/
/.codex/
/.trae/
.playwright-mcp/

# Generated by translate_file / build_dictionary — rebuilds on demand
/apps/web-server/translation-dictionaries/

# build_dictionary tests write notes next to the package root when DATA_DIR
# is unset; the artifacts are inert and noise on a clean checkout.
/translation-dictionaries/

# Local exec session artifacts (machine-specific)
/.run/
/packages/agent-skills/translation-dictionaries/

# translate_file side-products left next to fixtures
apps/docs/tests/pagination-corpus/docx/07-page-breaks*.docx

# Agent runtime state (not source)
/.pi/

# Auto-generated planning artifacts (pi-lens overnight runs).
# Each `plan-NN-*.md` is regenerated on every scan; committing them
# would create noisy churn and pull planning commentary into the tree.
/plan/
