const BASE='http://127.0.0.1:18081'
// trigger a preview generation by reading the html app's current preview: use the html:save-file + preview channel? Simplest: open via the app then read its iframe src is dynamic.
// Instead: find the preview handler and its injected script in source.
console.log('see source grep')
